/** The storage places both finders print. Each supplies its own UNKNOWN, which is not the same fact. */
export const STORAGE_PLACE_LABEL = {
  FLOOR: 'Dome floor',
  LAB: 'Summit lab',
  BASE: 'Base facility',
} as const;
